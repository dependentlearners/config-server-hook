server.port: 8888
spring:
    cloud:
        config :
            server:
                git:
                  uri: https://github.com/ankitggits/configuration-{profile}.git
logging:
    level:
        org.springframework.cloud: INFO

