spring:
    cloud:
        config :
            server.git.uri: file:///C://config-repo
            native:
            searchLocations: file:///C://config-repo
logging:
    level:
        org.springframework.cloud: DEBUG